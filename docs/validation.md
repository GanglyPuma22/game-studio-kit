# Baseline implementation validation — September 5, 2026

This section records the pre-audit baseline. The later loop-unit repair changes `main.gd`; historical tested-inputs.json and runtime-smoke.json remain untouched and do not attest the revised scene. See [audit revision validation](audit-improvements.md) and [current input identities](evidence/current-inputs.json).

Executed on Python **3.14.4**. Core syntax also parsed with the Python 3.11 grammar; native Python 3.11 execution remains unperformed.

- **59 standard-library tests passed after parent review.** Coverage includes relocation/reference closure, process ownership, missing capabilities, file/hash/stage/clip failures, content/workflow identity, evidence-method mismatch, all seven Meshy request profiles and interruption/archive cases, three hosted audio profiles, deterministic PCM preparation, terrain dimensions/seams and Gaea/app command boundaries. Provider tests use mocked transports; none spent credits.
- `check-package` passed, including ten entrypoints, internal Markdown/resource links, JSON templates and pinned adaptation hashes. Official plugin-manifest validation and all ten skill frontmatter validators passed using isolated validator dependencies. Ruff 0.15.7 format/check passed; Ruff/PyYAML are validation tools, not runtime dependencies.
- **Real Blender 5.0.1 Windows background** fixture creation, ten source renders, GLB export and fresh-process GLB import passed. The generic collection exporter and two-view turntable also passed. [Roundtrip](evidence/roundtrip.json): four meshes, three materials, two bones, idle/response actions, approximately 1.06 × 0.94 × 1.5 m in Blender axes. [Clip/source metadata](evidence/source-inspection.json) and [turntable settings](evidence/turntable.json) retained.
- **Real Godot 4.5.1 Linux headless** import and runtime smoke passed. [Runtime assertions](evidence/runtime-smoke.json): 2.458 m action-driven movement, one response, two imported clips/two bones, pose change, audio-player start and return to idle. [Tested runtime input hashes](evidence/tested-inputs.json) bind the report to the shipped scene/asset bytes. Audio driver was Dummy; this is not listening or ordinary-input evidence.
- A final committed archive was extracted into a path with spaces; package validation and all three example artifact records passed. A fresh copy of that **exact shipped example** passed Godot import/smoke with all seven retained scene/runtime hashes matched to the tested bytes. Regenerated Blender outputs are not assumed byte-identical.
- A complete copy in a path with spaces was checked and used to regenerate the fixture with isolated Python module loading, then import/run it. Unit tests also run a relocated absolute entrypoint from a different game directory with an empty test agent profile. These verify helper portability, not actual registered-plugin discovery.

Source images were visually inspected: [rest](evidence/response-001.png), [stress bend](evidence/response-013.png), and [quarter-turn](evidence/view-0090.00-frame-0001.png). The colored parts and bend are visible with no detached weighted mesh. Loop endpoint captures are retained ([start](evidence/idle-001.png), [end](evidence/idle-049.png)); renderer bytes differ, so there is no claim of byte-identical PNGs. This source-render inspection is separate from runtime/native motion acceptance.

Observed fixes during validation: exclude Blender-imported bone-display custom shapes from geometry bounds; apply WSL mappings only to Windows executables; isolate Godot profile paths from user settings; treat Godot logged script/import errors as failures; stop/release audio streams before headless shutdown and use real-time smoke execution to avoid lingering playback resources. No global application installation or visible desktop operation was performed.

Native Windows Python/Godot, actually registered ChatGPT/Codex coordinator invocation from a separate project cwd, computer use, normal controls, audible runtime mix, target GPU performance, live providers, entitled Gaea builds and Blender MCP pairing remain **not_run/unverified**. Follow [the exact Windows smoke prompt](windows-smoke.md). The production art pilot and any public release remain separate decisions; the functional fixture does not certify either.

Parent-review regressions cover explicit Godot smoke opt-in, unchanged shipped runtime hashes, required export template configuration with isolated copy/cleanup, task-specific Meshy archives that preserve conflicting files, and Gaea reserved-variable rejection before writes or execution. Export staging used fake templates and a mocked process; no live export, paid provider or native desktop run was added. The original fixture runtime bytes and their retained real-run evidence remain unchanged; only project metadata now declares its existing smoke protocol.
